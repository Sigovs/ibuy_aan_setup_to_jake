/* iBuy homepage JS — bundled from us-map.js + network.js + main.js (ES modules flattened for the AAN plain-script mockup). */
(function(){
"use strict";

/* ===== us-map.js (map geometry + cities) ===== */
/* ==========================================================================
   Точечная карта США — статические данные.

   Сгенерирована из реальной геометрии штатов (GeoJSON, континентальная часть),
   спроецирована линейно по долготе со сжатием по cos(средней широты) и просеяна
   сеткой с шагом 10px. Города спроецированы ТЕМ ЖЕ преобразованием — поэтому
   маркеры стоят ровно там, где нарисована страна, а не «на глаз».

   На сайте никакого fetch и никаких зависимостей: только эти числа.
   Пересобрать — см. README, раздел «Карта».
   ========================================================================== */

const MAP_W = 1000;
const MAP_H = 528;

/* Пары "x,y" через пробел — так в разы компактнее массива массивов. */
const MAP_DOTS = '40,10 50,10 60,10 70,10 80,10 90,10 100,10 110,10 120,10 130,10 140,10 150,10 160,10 170,10 180,10 190,10 200,10 210,10 220,10 230,10 240,10 250,10 260,10 270,10 280,10 290,10 300,10 310,10 320,10 330,10 340,10 350,10 360,10 370,10 380,10 390,10 400,10 410,10 420,10 430,10 440,10 450,10 460,10 470,10 480,10 490,10 500,10 510,10 40,20 50,20 60,20 70,20 80,20 90,20 100,20 110,20 120,20 130,20 140,20 150,20 160,20 170,20 180,20 190,20 200,20 210,20 220,20 230,20 240,20 250,20 260,20 270,20 280,20 290,20 300,20 310,20 320,20 330,20 340,20 350,20 360,20 370,20 380,20 390,20 400,20 410,20 420,20 430,20 440,20 450,20 460,20 470,20 480,20 490,20 500,20 510,20 520,20 530,20 540,20 550,20 10,30 20,30 30,30 50,30 60,30 70,30 80,30 90,30 100,30 110,30 120,30 130,30 140,30 150,30 160,30 170,30 180,30 190,30 200,30 210,30 220,30 230,30 240,30 250,30 260,30 270,30 280,30 290,30 300,30 310,30 320,30 330,30 340,30 350,30 360,30 370,30 380,30 390,30 400,30 410,30 420,30 430,30 440,30 450,30 460,30 470,30 480,30 490,30 500,30 510,30 520,30 530,30 540,30 550,30 560,30 570,30 580,30 590,30 600,30 620,30 10,40 20,40 30,40 40,40 50,40 60,40 70,40 80,40 90,40 100,40 110,40 120,40 130,40 140,40 150,40 160,40 170,40 180,40 190,40 200,40 210,40 220,40 230,40 240,40 250,40 260,40 270,40 280,40 290,40 300,40 310,40 320,40 330,40 340,40 350,40 360,40 370,40 380,40 390,40 400,40 410,40 420,40 430,40 440,40 450,40 460,40 470,40 480,40 490,40 500,40 510,40 520,40 530,40 540,40 550,40 560,40 570,40 580,40 10,50 20,50 30,50 40,50 50,50 60,50 70,50 80,50 90,50 100,50 110,50 120,50 130,50 140,50 150,50 160,50 170,50 180,50 190,50 200,50 210,50 220,50 230,50 240,50 250,50 260,50 270,50 280,50 290,50 300,50 310,50 320,50 330,50 340,50 350,50 360,50 370,50 380,50 390,50 400,50 410,50 420,50 430,50 440,50 450,50 460,50 470,50 480,50 490,50 500,50 510,50 520,50 530,50 540,50 550,50 560,50 570,50 620,50 960,50 970,50 980,50 20,60 30,60 40,60 50,60 60,60 70,60 80,60 90,60 100,60 110,60 120,60 130,60 140,60 150,60 160,60
,430 460,430 470,430 480,430 490,430 500,430 510,430 560,430 580,430 590,430 600,430 720,430 730,430 740,430 750,430 370,440 420,440 430,440 440,440 450,440 460,440 470,440 480,440 490,440 500,440 510,440 730,440 740,440 750,440 420,450 430,450 440,450 450,450 460,450 470,450 480,450 490,450 730,450 740,450 750,450 760,450 430,460 440,460 450,460 460,460 470,460 480,460 730,460 740,460 750,460 760,460 440,470 450,470 460,470 470,470 740,470 750,470 760,470 440,480 450,480 460,480 470,480 730,480 740,480 750,480 760,480 770,480 450,490 460,490 740,490 750,490 760,490 770,490 450,500 460,500 470,500 750,500 760,500 770,500 470,510 750,510 760,510 770,510 760,520';

/* Координаты в системе карты (не широта/долгота). */
const MAP_CITIES = {
  "cleveland": [
    748.1,
    172.9
  ],
  "baltimore": [
    830.2,
    222.7
  ],
  "beverlyHills": [
    109.2,
    333.3
  ],
  "newportBeach": [
    117.4,
    343
  ],
  "kansasCity": [
    521.9,
    223.8
  ]
};

/* ===== network.js (US map canvas + pins) ===== */
/* ==========================================================================
   SECTION 02 — NETWORK
   Точечная карта США + локации Marshall Goldman.

   Точки страны рисуем в <canvas> (их 3094 — в SVG это 3094 узла DOM, и любая
   анимация начинает лагать). Маркеры, дуги и подписи — SVG поверх: их мало,
   зато они интерактивны и масштабируются.
   ========================================================================== */


const REDUCED = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

const DOTS = MAP_DOTS.split(' ').map((p) => p.split(',').map(Number));

function initNetwork() {
  const root = document.getElementById('network');
  if (!root) return;

  const canvas = root.querySelector('.map__dots');
  const svg    = root.querySelector('.map__svg');
  const items  = [...root.querySelectorAll('.loc')];
  if (!canvas || !svg) return;

  const ctx = canvas.getContext('2d');

  /* --- Поле точек ---------------------------------------------------------
     Перерисовываем на resize: canvas растровый, и без учёта devicePixelRatio
     на retina точки превращаются в кашу. */

  const paintDots = () => {
    /* offsetWidth/offsetHeight, а НЕ getBoundingClientRect.

       Canvas лежит внутри наклонённой плоскости (rotateX). getBoundingClientRect
       возвращает СПРОЕЦИРОВАННЫЙ размер — сплющенный наклоном: высота приходила
       371 вместо 489. Битмап получался на четверть короче нужного, и весь юг
       страны — Техас, Флорида, залив — просто не рисовался: точки уезжали
       за нижний край битмапа. Карта выглядела чётко обрезанной, и никакая
       правка секции этого не лечила.

       offset* отдаёт РАЗМЕР В РАЗМЕТКЕ и трансформами не искажается. */
    const w = canvas.offsetWidth;
    const h = canvas.offsetHeight;
    if (!w || !h) return;

    const dpr = Math.min(window.devicePixelRatio || 1, 2);
    canvas.width  = Math.round(w * dpr);
    canvas.height = Math.round(h * dpr);

    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);
    ctx.clearRect(0, 0, w, h);

    const scale = w / MAP_W;
    const r = Math.max(1, 1.5 * scale);

    /* Цвет берём из токена, а не из литерала: canvas не знает про тему, и на
       светлой секции белые точки просто исчезли бы. Читаем на каждой отрисовке,
       поэтому смена темы подхватится сама. */
    ctx.fillStyle = getComputedStyle(root).getPropertyValue('--map-dot').trim()
                 || 'rgba(255, 255, 255, 0.26)';
    DOTS.forEach(([x, y]) => {
      ctx.beginPath();
      ctx.arc(x * scale, y * scale, r, 0, Math.PI * 2);
      ctx.fill();
    });
  };

  paintDots();

  let resizeTimer = null;
  window.addEventListener('resize', () => {
    clearTimeout(resizeTimer);
    resizeTimer = setTimeout(() => {
      paintDots();
      // Дуги привязаны к ЭКРАННЫМ координатам маркеров: изменился размер —
      // города уехали, и дуги надо перечертить, иначе они повиснут в стороне.
      if (currentKey) drawArcs(currentKey);
    }, 150);
  });


  /* --- Дуги ---------------------------------------------------------------
     Требование клиента №3: машины перегоняются между любыми локациями.

     Дуги живут в ОТДЕЛЬНОМ, НЕ наклонённом слое (.map__air) поверх плоскости.
     Если бы они были внутри наклонённого <svg>, то легли бы на землю крашеными
     полосами. Здесь они выгибаются НАД страной — и только тогда читаются как
     маршруты, а не как границы.

     Из-за этого концы дуг НЕЛЬЗЯ брать из MAP_CITIES: те координаты — в системе
     карты, а после rotateX город на экране оказывается совсем не там. Берём
     реальные экранные позиции маркеров (getBoundingClientRect уже учитывает
     3D-трансформ) и пересчитываем их в координаты слоя. */

  const air = root.querySelector('.map__air');
  const arcLayer = air.querySelector('.map__arcs');

  /* Экранная позиция города = центр его точки после наклона. */
  const screenPoint = (key) => {
    const dot = root.querySelector(`.marker[data-city="${key}"] .marker__dot`);
    if (!dot) return null;

    const r = dot.getBoundingClientRect();
    const box = air.getBoundingClientRect();
    return [r.left + r.width / 2 - box.left, r.top + r.height / 2 - box.top];
  };

  const arcPath = (a, b) => {
    const [x1, y1] = a;
    const [x2, y2] = b;
    const mx = (x1 + x2) / 2;
    const my = (y1 + y2) / 2;
    // Чем дальше города, тем выше летит маршрут.
    const lift = Math.hypot(x2 - x1, y2 - y1) * 0.26;
    return `M ${x1} ${y1} Q ${mx} ${my - lift} ${x2} ${y2}`;
  };

  /* Слой рисуем в ПИКСЕЛЯХ карты, а не в её внутренних координатах: экранные
     позиции маркеров приходят именно в пикселях. */
  const syncAirViewBox = () => {
    const box = air.getBoundingClientRect();
    if (box.width) air.setAttribute('viewBox', `0 0 ${box.width} ${box.height}`);
  };

  let currentKey = null;

  const drawArcs = (fromKey) => {
    currentKey = fromKey;
    arcLayer.innerHTML = '';
    syncAirViewBox();

    const from = screenPoint(fromKey);
    if (!from) return;

    Object.keys(MAP_CITIES).forEach((key) => {
      if (key === fromKey) return;

      const to = screenPoint(key);
      if (!to) return;

      const path = document.createElementNS('http://www.w3.org/2000/svg', 'path');
      path.setAttribute('d', arcPath(from, to));
      path.setAttribute('class', 'arc');
      arcLayer.appendChild(path);

      if (REDUCED) return;

      // Дуга прочерчивается, а не появляется целиком.
      const len = path.getTotalLength();
      path.style.strokeDasharray = len;
      path.style.strokeDashoffset = len;
      path.getBoundingClientRect();   // форсируем reflow, иначе анимации не будет
      path.style.transition = 'stroke-dashoffset 900ms cubic-bezier(0.16, 1, 0.30, 1)';
      path.style.strokeDashoffset = '0';
    });
  };

  const clearArcs = () => {
    currentKey = null;
    arcLayer.innerHTML = '';
  };


  /* --- Связка карты и списка ---------------------------------------------- */

  const markers = new Map();
  root.querySelectorAll('.marker').forEach((m) => markers.set(m.dataset.city, m));

  const panel = {
    root:    root.querySelector('.locpanel'),
    name:    root.querySelector('[data-slot="loc-name"]'),
    address: root.querySelector('[data-slot="loc-address"]'),
    phone:   root.querySelector('[data-slot="loc-phone"]'),
  };

  const select = (item) => {
    const key = item.dataset.city;

    items.forEach((i) => i.classList.toggle('is-active', i === item));
    markers.forEach((m, k) => m.classList.toggle('is-active', k === key));

    drawArcs(key);

    if (panel.root) {
      panel.name.textContent    = item.dataset.name;
      panel.address.textContent = item.dataset.address;

      // У новой локации ещё нет ни адреса, ни телефона — не выдумываем.
      if (item.dataset.phone) {
        panel.phone.textContent = item.dataset.phone;
        panel.phone.href = 'tel:' + item.dataset.phone.replace(/\D/g, '');
        panel.phone.hidden = false;
      } else {
        panel.phone.hidden = true;
      }

      panel.root.classList.add('is-open');
    }
  };

  const reset = () => {
    items.forEach((i) => i.classList.remove('is-active'));
    markers.forEach((m) => m.classList.remove('is-active'));
    clearArcs();
    if (panel.root) panel.root.classList.remove('is-open');
  };


  /* --- Автоперебор ---------------------------------------------------------
     Пока никто не трогает список, карта сама медленно обходит локации:
     Cleveland → Baltimore → Beverly Hills → ... Иначе в покое секция мёртвая,
     и человек может вообще не понять, что с ней можно взаимодействовать.

     DWELL великоват намеренно: дуга прочерчивается 900ms, и ей надо дать
     постоять. Быстрый перебор превращается в мигание. */

  const DWELL = 4200;

  let timer = null;
  let index = 0;
  let onScreen = false;
  let engaged = false;   // человек взял управление на себя

  const step = () => {
    select(items[index % items.length]);
    index += 1;
  };

  const canRun = () => onScreen && !engaged && !document.hidden && !REDUCED;

  const startAuto = () => {
    stopAuto();
    if (!canRun()) return;

    step();                                  // сразу, без ожидания первого тика
    timer = setInterval(() => {
      if (!canRun()) return stopAuto();
      step();
    }, DWELL);
  };

  const stopAuto = () => {
    clearInterval(timer);
    timer = null;
  };

  // Не крутим перебор, когда секции нет на экране: она в самом низу страницы,
  // и без этого таймер жёг бы процессор всё время, что человек читает герой.
  const io = new IntersectionObserver((entries) => {
    onScreen = entries[0].isIntersecting;
    if (onScreen) {
      startAuto();
    } else {
      stopAuto();
      if (!engaged) reset();
    }
  }, { threshold: 0.25 });

  io.observe(root);

  // Скрытая вкладка — таймер не нужен.
  document.addEventListener('visibilitychange', () => {
    if (document.hidden) stopAuto();
    else startAuto();
  });


  /* --- Человек всегда важнее автоперебора ---------------------------------- */

  let resumeTimer = null;

  /* Возврат автоперебора НЕ должен зависеть только от mouseleave.
     На тачскрине его не существует: человек тапнул локацию — и карта замерла бы
     навсегда. Поэтому любое вмешательство всегда заводит таймер возврата,
     а уход курсора просто укорачивает его. */
  const scheduleResume = (delay) => {
    clearTimeout(resumeTimer);
    resumeTimer = setTimeout(() => {
      engaged = false;
      startAuto();
    }, delay);
  };

  const takeOver = (item) => {
    engaged = true;
    stopAuto();
    select(item);

    // Перебор продолжится со СЛЕДУЮЩЕЙ за выбранной, а не с того места, где
    // его прервали: иначе после ухода курсора карта прыгнет обратно к городу,
    // который человек только что посмотрел.
    index = items.indexOf(item) + 1;

    // Страховка на случай, когда mouseleave не придёт вообще (тач).
    scheduleResume(9000);
  };

  items.forEach((item) => {
    item.addEventListener('mouseenter', () => takeOver(item));
    item.addEventListener('focus', () => takeOver(item));
    item.addEventListener('click', () => takeOver(item));   // тач: наведения нет
  });

  // Уход со ВСЕГО списка, а не с одной строки: иначе при переходе между
  // соседними панель успевает мигнуть.
  const list = root.querySelector('.locs');

  if (list) {
    // Пауза перед возвратом: если карта дёрнется в ту же секунду, как курсор
    // ушёл, это читается как сбой, а не как «она снова сама».
    list.addEventListener('mouseleave', () => scheduleResume(1600));
    list.addEventListener('focusout', (e) => {
      if (!list.contains(e.relatedTarget)) scheduleResume(1600);
    });
  }
}

/* ===== main.js (reveals, counters, animations) ===== */
/* ==========================================================================
   iBuy — main.js
   Пока только то, что нужно герою. Растёт вместе с секциями.
   ========================================================================== */


(() => {
  'use strict';

  const REDUCED = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  initNetwork();   // SECTION 02


  /* --- Набег цифр -----------------------------------------------------------
     Раньше значение мерцало случайными числами и вставало на место — читалось
     как глюк, а не как счёт. Теперь оно НАБЕГАЕТ к финалу с экспоненциальным
     затуханием: быстро стартует, мягко доезжает. Рывка в конце нет.

     onFrame получает текущее значение — вызывающий сам решает, как его писать
     (в прибор, на землю, в оба места сразу). */

  const easeOutExpo = (t) => (t === 1 ? 1 : 1 - Math.pow(2, -10 * t));

  const countTo = (from, to, duration, onFrame, onDone) => {
    if (REDUCED) {
      onFrame(to);
      if (onDone) onDone();
      return () => {};
    }

    let raf = null;
    const start = performance.now();

    const step = (now) => {
      const t = Math.min(1, (now - start) / duration);
      onFrame(from + (to - from) * easeOutExpo(t));

      if (t < 1) {
        raf = requestAnimationFrame(step);
      } else if (onDone) {
        onDone();
      }
    };

    raf = requestAnimationFrame(step);
    return () => cancelAnimationFrame(raf);   // отмена, если ввод продолжился
  };

  /* --- Шапка: садится на плотный фон, как только ушли с первого экрана ----- */

  const header = document.getElementById('header');

  if (header) {
    const onScroll = () => header.classList.toggle('is-stuck', window.scrollY > 24);
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
  }


  /* --- Пейзаж за маршрутом: медленно выезжает снизу на скролле --------------
     Горы дрейфуют вверх по мере прохода секции — тихий параллакс, а не рывок.
     Считаем прогресс по центру рельса относительно центра экрана: внизу экрана
     кадр опущен (+56px), к центру встаёт на место. --rail-shift читает CSS. */

  const railBg = document.querySelector('.rail__bg img');

  if (railBg && !REDUCED) {
    const rail = railBg.closest('.rail');
    let raf = 0;

    const update = () => {
      raf = 0;
      const r = rail.getBoundingClientRect();
      if (r.bottom < 0 || r.top > window.innerHeight) return;   // за кадром — не трогаем
      const mid = r.top + r.height / 2;
      // 1 у нижнего края экрана → 0 в центре и выше. Кадр выезжает снизу на 120px
      // и одновременно проявляется (тусклее на входе, полный в покое) — тогда
      // «слайд снизу» читается, а не просто сдвиг.
      const p = Math.max(0, Math.min(1, (mid - window.innerHeight * 0.42) / (window.innerHeight * 0.58)));
      railBg.style.setProperty('--rail-shift', (p * 120).toFixed(1) + 'px');
      railBg.style.setProperty('--rail-fade', (0.85 - p * 0.55).toFixed(3));
    };

    const onScroll = () => { if (!raf) raf = requestAnimationFrame(update); };
    window.addEventListener('scroll', onScroll, { passive: true });
    window.addEventListener('resize', onScroll);
    update();
  }


  /* --- Рендеры шагов: 3D-наклон за курсором ---------------------------------
     Та же 3D-симуляция, что у карты и телефона: рендер наклоняется вслед за
     курсором в своей колонке. Перспектива задана в CSS, JS ведёт только углы
     --rx/--ry, которые складываются с лифтом от ховера в один transform.

     Наклон небольшой (±7°): предмет и так «объёмный» (телефон + машина парят),
     сильный поворот сломал бы его тени. Только hover-устройства и не в reduced. */

  if (!REDUCED && window.matchMedia('(hover: hover)').matches) {
    const RANGE = 7;
    document.querySelectorAll('.step__media--plate').forEach((media) => {
      const img = media.querySelector('img');
      if (!img) return;
      let raf = 0;

      const tilt = (e) => {
        if (raf) return;
        raf = requestAnimationFrame(() => {
          raf = 0;
          const r = media.getBoundingClientRect();
          const dx = (e.clientX - (r.left + r.width / 2)) / (r.width / 2);
          const dy = (e.clientY - (r.top + r.height / 2)) / (r.height / 2);
          const clamp = (v) => Math.max(-1, Math.min(1, v));
          img.style.setProperty('--ry', `${clamp(dx) * RANGE}deg`);
          img.style.setProperty('--rx', `${-clamp(dy) * RANGE}deg`);
        });
      };

      media.addEventListener('mousemove', tilt);
      media.addEventListener('mouseleave', () => {
        img.style.setProperty('--ry', '0deg');
        img.style.setProperty('--rx', '0deg');
      });
    });
  }


  /* --- Появление лесенкой ---------------------------------------------------
     Заголовки выезжают сбоку, текст и блоки — снизу. Шаг задаёт --rise-i.

     Индекс ставится НЕ заранее, а в момент входа в кадр — и только среди тех
     элементов, что вошли одной пачкой (сверху вниз). Если раздать индексы
     заранее, элемент с индексом 3, доехавший до кадра в одиночку, честно
     отстоит свои 390мс и только потом появится — выглядит как подвисание.
     Так лесенка играет там, где секция въезжает целиком, и не мешает там, где
     человек листает медленно. */

  /* --- Счётчики -------------------------------------------------------------
     Любой [data-count] набегает к своему числу, когда входит в кадр. Тот же
     easeOutExpo, что у живой оценки: быстро стартует, мягко доезжает.

     Ширину поля держат табличные цифры (CSS), иначе «20 min» дёргается на
     каждом кадре: у пропорциональной единицы ширина меньше нуля. */

  const counters = document.querySelectorAll('[data-count]');

  if (counters.length) {
    const count = new IntersectionObserver((entries) => {
      entries
        .filter((e) => e.isIntersecting)
        .forEach((e, i) => {
          const to = parseFloat(e.target.dataset.count);
          count.unobserve(e.target);

          // data-money — деньги: с долларом и разрядами. Без этого порог
          // набегал бы как «100000» и читался бы как номер, а не как сумма.
          const money = 'money' in e.target.dataset;
          const step  = money ? 1000 : 1;
          const write = (v) => {
            const n = Math.round(v / step) * step;
            e.target.textContent = money ? '$' + n.toLocaleString('en-US') : String(n);
          };

          // Задержка в шаг лесенки: цифры должны догонять свой блок, а не
          // стартовать раньше него.
          setTimeout(() => {
            countTo(0, to, money ? 1600 : 1200, write);
          }, 260 + i * 130);
        });
    }, { threshold: 0.6 });

    counters.forEach((el) => count.observe(el));
  }


  const risers = document.querySelectorAll('[data-rise]');

  if (risers.length) {
    const rise = new IntersectionObserver((entries) => {
      // Вошедшие одной пачкой нумеруем сверху вниз — это и даёт лесенку: заголовок,
      // за ним подзаголовок, за ним блоки, по одному.
      const entering = entries
        .filter((e) => e.isIntersecting)
        .sort((a, b) => a.boundingClientRect.top - b.boundingClientRect.top);
      entering.forEach((e, i) => {
        e.target.style.setProperty('--rise-i', i);
        e.target.classList.add('is-in');
      });

      // Ушедшие за кадр СБРАСЫВАЕМ — тогда лесенка играет снова при следующем
      // заходе. Раньше стоял unobserve, и появление было одноразовым; клиент
      // хочет, чтобы оно повторялось всегда. Сбрасываем только когда элемент
      // полностью вышел, иначе он мигал бы на самом краю кадра.
      entries
        .filter((e) => !e.isIntersecting && e.boundingClientRect.top > 0)
        .forEach((e) => e.target.classList.remove('is-in'));
    }, { threshold: 0.12, rootMargin: '0px 0px -6% 0px' });

    risers.forEach((el) => rise.observe(el));
  }


  /* ==========================================================================
     ПРИБОР
     Одно место, три состояния — иначе ховер по ленте и живая оценка начинают
     драться за один и тот же экран:

       default  — общие цифры, покой
       car      — телеметрия машины из ленты (ховер / тап / фокус)
       estimate — живая оценка, пока человек набирает форму

     Приоритет у estimate: если человек уже вводит свою машину, перебивать
     его оценку случайным ховером нельзя.
     ========================================================================== */

  const readout = document.getElementById('readout');
  const form    = document.getElementById('quote-form');
  const lineup  = document.getElementById('lineup');

  /* Обязательна только ФОРМА. Прибор и лента сейчас закомментированы в разметке
     (на их месте trust bar), но живая оценка на бетоне обязана продолжать
     работать — она к ним не привязана. Поэтому readout и lineup здесь
     необязательные: раскомментируют разметку — всё оживёт само, без правок JS. */
  if (form) {

    const pick = (root, sel) => (root ? root.querySelector(sel) : null);

    const panes = {
      default:  pick(readout, '[data-pane="default"]'),
      car:      pick(readout, '[data-pane="car"]'),
      estimate: pick(readout, '[data-pane="estimate"]'),
    };

    const car = {
      name:  pick(readout, '[data-slot="name"]'),
      paid:  pick(readout, '[data-slot="paid"]'),
      where: pick(readout, '[data-slot="where"]'),
      time:  pick(readout, '[data-slot="time"]'),
    };

    const est = {
      status:  pick(readout, '[data-slot="status"]'),
      range:   pick(readout, '[data-slot="range"]'),
      compare: pick(readout, '[data-slot="compare"]'),
    };

    const items = lineup ? [...lineup.querySelectorAll('.lineup__item')] : [];

    /* Земля: телеметрия, лежащая на бетоне перед машинами. Ведётся тем же
       состоянием, что и прибор, — иначе они начнут показывать разное. */
    const ground = {
      root:   document.querySelector('.hero__ground'),
      label:  document.querySelector('[data-slot="ground-label"]'),
      figure: document.querySelector('[data-slot="ground-figure"]'),
      sub:    document.querySelector('[data-slot="ground-sub"]'),
    };

    /* Надпись на земле не подменяется, а ПЕРЕЗАПИСЫВАЕТСЯ: старая уплывает
       вглубь площадки, новая выступает из неё. Подмена текста происходит
       в паузе, когда старое уже невидимо, — иначе глаз ловит рывок. */
    let swapTimer = null;

    const writeGround = (label, figure, sub, working, afterSwap) => {
      // Земли может не быть в разметке — счёт всё равно обязан пойти,
      // иначе прибор навсегда застрянет на «Computing».
      if (!ground.root) {
        if (afterSwap) afterSwap();
        return;
      }

      const apply = () => {
        ground.label.textContent = label;
        ground.sub.textContent   = sub;

        // figure === null означает «цифру ведёт счётчик» — не трогаем её,
        // иначе apply() затрёт значение, уже набегающее на этом кадре.
        if (figure !== null) ground.figure.textContent = figure;

        ground.root.classList.add('is-live');
        ground.root.classList.toggle('is-working', Boolean(working));
        ground.root.classList.remove('is-swapping');
        if (afterSwap) afterSwap();
      };

      if (REDUCED) { apply(); return; }

      clearTimeout(swapTimer);
      ground.root.classList.add('is-swapping');
      // 220ms — сколько букве нужно, чтобы уйти в бетон (см. --t-base в CSS).
      swapTimer = setTimeout(apply, 220);
    };

    /* Отдельно от writeGround: во время набега цифр перезаписывать плоскость
       нельзя — иначе она будет мигать на каждом кадре. Пишем прямо в узел. */
    const setGroundFigure = (text) => {
      if (ground.figure) ground.figure.textContent = text;
    };

    /* Земля молчит в покое. Дежурная надпись про сеть была дублем секции 02
       (там это делает настоящая карта) и просто лежала поперёк машин.
       Теперь бетон оживает ТОЛЬКО под цифру — иначе он украшение, а не реакция. */
    const hideGround = () => {
      if (ground.root) ground.root.classList.remove('is-live', 'is-working');
    };

    // Пока true — форма ведёт прибор, лента его не трогает.
    let estimating = false;

    const showPane = (name) => {
      Object.entries(panes).forEach(([key, el]) => {
        if (el) el.classList.toggle('is-active', key === name);
      });
    };

    const markItem = (item) => {
      items.forEach((i) => i.classList.toggle('is-active', i === item));
    };


    /* --- Состояние «машина из ленты» --------------------------------------- */

    const money   = (n) => '$' + (Math.round(n / 1000) * 1000).toLocaleString('en-US');
    const unmoney = (s) => Number(String(s).replace(/[^0-9]/g, '')) || 0;

    /* Счётчиков одновременно два — прибор и земля. Прибор стартует сразу,
       земля дожидается перезаписи (буквы должны сперва уйти в бетон), поэтому
       она отстаёт на 220ms. Отменять надо оба разом: перескочил на соседнюю
       машину — старые счётчики не должны дописывать чужую сумму. */
    let cancels = [];

    const cancelCount = () => {
      cancels.forEach((stop) => stop());
      cancels = [];
    };

    const track = (stop) => cancels.push(stop);

    const showCar = (item) => {
      if (estimating) return;

      cancelCount();
      markItem(item);

      car.name.innerHTML    = item.dataset.name;   // в data-name лежат &mdash; и &middot;
      car.where.textContent = item.dataset.where;
      car.time.textContent  = item.dataset.time;
      showPane('car');

      const paid = unmoney(item.dataset.paid);

      // Прибор считает сразу — иначе он 220ms показывает пустоту.
      track(countTo(0, paid, 1100, (v) => { car.paid.textContent = money(v); }));

      // Земля — после того, как старая надпись ушла в бетон.
      writeGround(
        'We paid',
        null,   // цифру ведёт счётчик
        item.textContent.trim() + ' · ' + item.dataset.where,
        false,
        () => track(countTo(0, paid, 1100, (v) => setGroundFigure(money(v)))),
      );
    };

    const resetToDefault = () => {
      if (estimating) return;
      cancelCount();
      markItem(null);
      showPane('default');
      hideGround();
    };

    items.forEach((item) => {
      item.addEventListener('mouseenter', () => showCar(item));
      item.addEventListener('focus', () => showCar(item));
      item.addEventListener('click', () => showCar(item));   // тач: наведения нет
    });

    // Сбрасываем при уходе со ВСЕЙ ленты, а не с одной машины: иначе прибор
    // мигал при переходе между соседними.
    if (lineup) {
      lineup.addEventListener('mouseleave', resetToDefault);
      lineup.addEventListener('focusout', (e) => {
        if (!lineup.contains(e.relatedTarget)) resetToDefault();
      });
    }


    /* --- Состояние «живая оценка» ------------------------------------------
       ⚠️ ТАБЛИЦА ЦЕН — ЗАГЛУШКА. Нужна настоящая модель оценки от клиента
       или эндпоинт. Показывать живому продавцу выдуманную цифру нельзя —
       это ровно то, за что мы ругали старый сайт. */

    const MARKET = {
      ferrari:        { '812': 289000, 'f8': 275000, '296': 310000, 'roma': 215000, 'sf90': 480000, _base: 260000 },
      lamborghini:    { 'revuelto': 561000, 'huracan': 245000, 'urus': 225000, 'aventador': 410000, _base: 300000 },
      porsche:        { 'gt3': 237000, '911': 210000, 'turbo': 215000, 'taycan': 95000, _base: 150000 },
      mclaren:        { '720s': 258000, '765lt': 400000, 'artura': 230000, _base: 240000 },
      mercedes:       { 'amg gt': 342000, 'g63': 180000, 'sl': 130000, _base: 160000 },
      'aston martin': { 'db12': 245000, 'vantage': 165000, _base: 190000 },
      bentley:        { 'continental': 210000, 'bentayga': 195000, _base: 200000 },
      'rolls-royce':  { 'cullinan': 375000, 'ghost': 340000, 'wraith': 290000, _base: 330000 },
    };

    // Совпадение по вхождению, а не по равенству: человек пишет «812 Superfast»,
    // в таблице ключ «812».
    const lookup = (make, model) => {
      const makeKey = Object.keys(MARKET).find((k) => make.includes(k) || k.includes(make));
      if (!makeKey) return null;

      const table = MARKET[makeKey];
      const modelKey = Object.keys(table)
        .filter((k) => k !== '_base')
        .find((k) => model.includes(k) || (model.length > 2 && k.includes(model)));

      return modelKey ? table[modelKey] : table._base;
    };

    // Возраст и пробег давят на цену. Пол — 35% от базы: ниже уходить нечестно.
    const adjust = (base, year, miles) => {
      const age = Math.max(0, new Date().getFullYear() - (year || new Date().getFullYear()));
      let v = base * Math.pow(0.96, age);
      if (miles > 2000) v *= Math.max(0.62, 1 - ((miles - 2000) / 1000) * 0.011);
      return Math.max(base * 0.35, v);
    };

    // Похожая машина в ленте — доказательство рядом с оценкой.
    const findTwin = (make, model) => items.find((i) => {
      const n = i.dataset.name.toLowerCase();
      return n.includes(make) && (model.length > 2 ? n.includes(model) : true);
    });

    const compute = () => {
      const make  = form.querySelector('#q-make').value.trim().toLowerCase();
      const model = form.querySelector('#q-model').value.trim().toLowerCase();
      const year  = parseInt(form.querySelector('#q-year').value, 10);
      const miles = parseInt(form.querySelector('#q-mileage').value.replace(/\D/g, ''), 10) || 0;

      const base = make ? lookup(make, model) : null;

      // Марку не знаем — молча возвращаемся в покой, без «не найдено».
      if (!base) {
        cancelCount();
        estimating = false;
        markItem(null);
        showPane('default');
        hideGround();
        return;
      }

      cancelCount();
      estimating = true;
      markItem(null);
      showPane('estimate');

      if (est.status) {
        est.status.textContent = 'Computing';
        est.status.classList.add('is-working');
      }
      if (est.compare) est.compare.textContent = '';

      const label = [year || '', make, model].filter(Boolean).join(' ').toUpperCase();

      const mid = adjust(base, year, miles);
      const lo  = mid * 0.95;
      const hi  = mid * 1.06;

      const settle = () => {
        if (est.status) {
          est.status.textContent = 'Live';
          est.status.classList.remove('is-working');
        }

        const twin = findTwin(make, model);
        if (twin) markItem(twin);

        if (!est.compare) return;
        est.compare.innerHTML = twin
          ? 'We paid <b>' + twin.dataset.paid + '</b> for a ' +
            twin.textContent.trim() + ' &mdash; ' + twin.dataset.where
          : 'Based on <b>iBuy</b> purchases across 5 locations';
      };

      // Обе границы диапазона набегают вместе, пропорционально — иначе нижняя
      // и верхняя ползут вразнобой и читаются как ошибка.
      const range = (v) => {
        const ratio = hi === 0 ? 1 : v / hi;
        return money(lo * ratio) + ' – ' + money(v);
      };

      // Прибор считает сразу. Если он отключён — счёт всё равно нужен: на нём
      // висит settle(), а землю ведёт отдельный счётчик ниже.
      track(countTo(0, hi, 1100, (v) => {
        if (est.range) est.range.textContent = range(v);
      }, settle));

      // Земля — после перезаписи; она же снимает мерцание, когда досчитает.
      writeGround('Your estimated offer', null, label, true, () => {
        track(countTo(
          0, hi, 1100,
          (v) => setGroundFigure(range(v)),
          () => ground.root && ground.root.classList.remove('is-working'),
        ));
      });
    };

    let debounce = null;
    ['#q-make', '#q-model', '#q-year', '#q-mileage'].forEach((sel) => {
      form.querySelector(sel).addEventListener('input', () => {
        clearTimeout(debounce);
        debounce = setTimeout(compute, 260);
      });
    });
  }


  /* --- Телефон: 3D-корпус и сцена «пять звёзд → галочка» --------------------
     Экран не листает отзывы (они уже набраны слева — это было бы дублирование),
     а показывает результат: звёзды зажигаются по одной, собираются, и на их
     месте дорисовывается галочка. Потом цикл повторяется.

     Вся анимация живёт в CSS-переходах, JS только переключает состояние. Значит
     prefers-reduced-motion гасит её сам: в reduced длительности сведены к 1мс,
     и переключение читается как мгновенная смена картинки. Поэтому в reduced мы
     просто не запускаем цикл и оставляем звёзды. */

  const verdict = document.getElementById('verdict');

  if (verdict) {
    const phone = verdict.closest('.phone');
    const frame = phone.querySelector('.phone__frame');

    /* Цикл крутится непрерывно, пока телефон в кадре. Паузы на наведении тут
       НЕТ — и это осознанно: чтобы разглядеть наклон, курсор ведут прямо на
       телефон, и анимация замирала ровно в тот момент, когда на неё смотрят.
       Читать здесь нечего (это не лента отзывов), останавливать нечего.

       Остаются два правила карты: не крутиться за кадром и при скрытой вкладке. */
    let onScreen = false;
    let timer = null;

    const canRun = () => onScreen && !document.hidden && !REDUCED;

    const stop = () => { clearTimeout(timer); timer = null; };


    /* Цифры. 5.0 набегает, пока зажигаются звёзды; 95 — пока обводится кольцо.
       Кольцо тогда читается как «просмотрено 95 отзывов», а галочка — как итог.
       Счётчик тот же, что у живой оценки в герое: экспоненциальное затухание. */

    const scoreEl = document.getElementById('verdict-score');
    const countEl = document.getElementById('verdict-count');
    let cancelScore = () => {};
    let cancelCountUp = () => {};

    const runScore = () => {
      cancelScore();
      // 5 звёзд × 110мс задержки + посадка последней — счёт должен закончиться
      // вместе с ними, а не раньше.
      cancelScore = countTo(0, 5, 900, (v) => { scoreEl.textContent = v.toFixed(1); });
    };

    const runCount = () => {
      cancelCountUp();
      // Стартуем с задержкой кольца (140мс) и идём его длительностью (700мс).
      const id = setTimeout(() => {
        cancelCountUp = countTo(0, 95, 700, (v) => {
          countEl.textContent = String(Math.round(v));
        });
      }, 140);
      cancelCountUp = () => clearTimeout(id);
    };

    // Звёзды держим 2.4с (успеть досчитать до пяти), галочку — 2.8с: это
    // финальный кадр, ему нужно дать постоять.
    const step = (toCheck) => {
      if (!canRun()) return stop();
      verdict.classList.toggle('is-check', toCheck);
      verdict.classList.toggle('is-stars', !toCheck);
      toCheck ? runCount() : runScore();
      timer = setTimeout(() => step(!toCheck), toCheck ? 2800 : 2400);
    };

    const start = () => {
      stop();
      if (!canRun()) return;
      runScore();
      timer = setTimeout(() => step(true), 2400);
    };

    // Порог низкий: сцена высокая, и на 0.4 она «невидима» даже когда телефон
    // уже наполовину в кадре — цикл просто не запускался.
    new IntersectionObserver((entries) => {
      onScreen = entries[0].isIntersecting;
      onScreen ? start() : stop();
    }, { threshold: 0.15 }).observe(verdict);

    document.addEventListener('visibilitychange', () => {
      document.hidden ? stop() : start();
    });


    /* Наклон. Углы ведут ДВА источника: курсор и скролл. Считаем их в одном
       месте и складываем — иначе они дерутся за одни и те же --ry/--rx и
       последний записавший стирает чужой вклад.

       Углы покоя продублированы здесь и в CSS. Так задумано: в CSS они держат
       телефон наклонённым там, где JS молчит (тач, reduced-motion), а здесь
       служат нулём, от которого считается отклонение. */

    if (!REDUCED) {
      const REST_RY = -15;
      const REST_RX = 7;
      const PTR_RANGE = 12;      // ±12° от курсора
      const SCROLL_RANGE = 9;    // ±9° за проход секции

      let ptrX = 0, ptrY = 0;    // -1..1
      let scrollT = 0;           // -1..1
      let raf = 0;

      const clamp = (v) => Math.max(-1, Math.min(1, v));

      const render = () => {
        raf = 0;
        const ry = REST_RY + ptrX * PTR_RANGE + scrollT * (SCROLL_RANGE * 0.35);
        const rx = REST_RX - ptrY * PTR_RANGE + scrollT * SCROLL_RANGE;
        frame.style.setProperty('--ry', `${ry.toFixed(2)}deg`);
        frame.style.setProperty('--rx', `${rx.toFixed(2)}deg`);
      };

      const schedule = () => { if (!raf) raf = requestAnimationFrame(render); };

      // Скролл: телефон доворачивается по мере прохода мимо. Работает и там,
      // где курсора нет вообще — на тач-устройствах 3D иначе просто мёртв.
      const onScroll = () => {
        if (!onScreen) return;
        const r = frame.getBoundingClientRect();
        const mid = r.top + r.height / 2;
        // 0 в центре экрана, ±1 у краёв: телефон «кивает», проходя кадр.
        scrollT = clamp((mid - window.innerHeight / 2) / (window.innerHeight / 2));
        schedule();
      };

      window.addEventListener('scroll', onScroll, { passive: true });
      window.addEventListener('resize', onScroll);
      onScroll();

      // Курсор — только там, где он есть. Слушаем всю секцию: телефон должен
      // «замечать» руку заранее, а не только когда на него навели.
      if (window.matchMedia('(hover: hover)').matches) {
        const section = document.getElementById('reviews');

        section.addEventListener('mousemove', (e) => {
          const r = frame.getBoundingClientRect();
          // Нормируем на два радиуса: курсор в метре от телефона не должен
          // класть его набок.
          ptrX = clamp((e.clientX - (r.left + r.width / 2)) / (r.width * 2));
          ptrY = clamp((e.clientY - (r.top + r.height / 2)) / (r.height * 2));
          schedule();
        });

        section.addEventListener('mouseleave', () => {
          ptrX = ptrY = 0;       // возвращаем к покою, но скролл продолжает вести
          schedule();
        });
      }
    }
  }


  /* ==========================================================================
     ФОРМА
     Валидация ручная, а не через :invalid — нативная красит поле ещё до
     первого ввода. Ошибку показываем после submit и снимаем сразу, как
     человек начал править.
     ========================================================================== */

  if (form) {
    const showError = (input, message) => {
      const slot = form.querySelector(`[data-error-for="${input.id}"]`);
      input.setAttribute('aria-invalid', 'true');
      if (slot) slot.textContent = message;
    };

    const clearError = (input) => {
      const slot = form.querySelector(`[data-error-for="${input.id}"]`);
      input.removeAttribute('aria-invalid');
      if (slot) slot.textContent = '';
    };

    const validate = (input) => {
      const value = input.value.trim();

      if (input.required && !value) {
        return `${input.previousElementSibling.textContent.trim()} is required`;
      }

      if (input.id === 'q-year' && value) {
        const year = Number(value);
        const max = new Date().getFullYear() + 2;   // допускаем модельный год вперёд
        if (!Number.isInteger(year) || year < 1950 || year > max) {
          return `Enter a year between 1950 and ${max}`;
        }
      }

      if (input.id === 'q-vin' && value && value.length !== 17) {
        return 'A VIN is 17 characters';
      }

      return null;
    };

    const inputs = [...form.querySelectorAll('.field__input')];

    form.addEventListener('submit', (event) => {
      event.preventDefault();

      let firstBad = null;

      inputs.forEach((input) => {
        const message = validate(input);
        if (message) {
          showError(input, message);
          if (!firstBad) firstBad = input;
        } else {
          clearError(input);
        }
      });

      if (firstBad) {
        firstBad.focus();
        return;
      }

      // TODO: шаг 2 (Location) и шаг 3 (Name / Email / Phone).
      // Куда уходит заявка — вопрос к клиенту: сейчас на сайте Contact Form 7.
      console.log('Step 1 OK', Object.fromEntries(new FormData(form)));
    });

    inputs.forEach((input) => {
      input.addEventListener('input', () => {
        if (input.hasAttribute('aria-invalid')) clearError(input);
      });
    });
  }

})();

})();
